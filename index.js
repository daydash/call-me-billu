const callMeBillu = (name) => {
	console.log(`Hello, ${name} billu.`);
};

const callMe = (name) => {
	console.log(`Jai Shree Ram, ${name} Ji 🙏 🚩`);
};

const dontCallMe = (name) => {
	console.log(`Okay bye ${name}!`);
};

module.exports = { callMeBillu, callMe, dontCallMe };
